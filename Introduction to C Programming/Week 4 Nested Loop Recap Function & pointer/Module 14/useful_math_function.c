#include<stdio.h>
#include<math.h>
int main()
{
    // int ans = ceil(4.3);
    // int ans = floor(3.9);
    // int ans = round(3.9);
    // int ans = sqrt(25);
    // double ans = sqrt(25);
    // int ans = pow(3,2);
    int ans = abs(-10);
    printf("%d",ans);
    return 0;
}